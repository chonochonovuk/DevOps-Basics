1.Docker Installation on Guest OS - Ubuntu-server 20.04
  -> vagrant init --minimal
  -> add one block, between the config.vm.box and end
  -> vagrant up & ssh
  -> Following docker Installation Steps as described in Docker Docs for Ubuntu
       -> Update the apt package index and install packages to allow apt to use a repository over HTTPS
       -> Add Docker’s official GPG key
       -> set up the stable repository
       -> Docker Installation
       -> Test & Status & Info Check
       -> add our user to the docker group
